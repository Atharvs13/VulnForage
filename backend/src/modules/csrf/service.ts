import {db} from '../../database/index.js'; import {assert} from '../../utils/errors.js'; import {logEvent} from '../../services/log.service.js';
export function changeEmail(userId:number,email:string){assert(/^[^@\s]+@[^@\s]+\.[^@\s]+$/.test(email),422,'INVALID_EMAIL','Valid synthetic email required');db().prepare(`INSERT INTO lab_settings(key,value) VALUES (?,?) ON CONFLICT(key) DO UPDATE SET value=excluded.value,updated_at=CURRENT_TIMESTAMP`).run(`contact_email_user_${userId}`,email);logEvent('LAB_CSRF_CHANGED',{userId,metadata:{email}});return {email,csrfTokenRequired:false};}
