# Logging & Alerting Failures Lab (A09:2025)

Target: `POST /api/lab/logging/event`

Demonstrates CRLF log injection and unmonitored security alerts where user input is written to log output streams without escaping newlines.
