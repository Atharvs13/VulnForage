# File upload lab
The filename check accepts any name containing `.txt`, including misleading double extensions. Files are random-named, non-executable, size-limited, and served as `text/plain` from dedicated storage.
