# Remediation
Pin an allowed algorithm, verify every signature using a managed key, validate issuer/audience/expiry, and perform authorization from trusted server state rather than token claims alone.

