import{Navigate,Route,Routes}from'react-router-dom';import{useAuth}from'./AuthContext';import{Layout}from'../components/Layout';import{Loading}from'../components/Status';import{Landing,Login,Register}from'../pages/AuthPages';import{Dashboard,Products,ProductDetail,Orders,OrderDetail,Support,Profile,Checkout}from'../pages/CorePages';import{Missions,MissionDetail}from'../pages/MissionPages';import{Admin}from'../pages/AdminPage';
function Protected(){const{user,loading}=useAuth();if(loading)return <div className="p-10"><Loading/></div>;return user?<Layout/>:<Navigate to="/login" replace/>}
export function App(){return <Routes><Route path="/" element={<Landing/>}/><Route path="/login" element={<Login/>}/><Route path="/register" element={<Register/>}/><Route element={<Protected/>}><Route path="dashboard" element={<Dashboard/>}/><Route path="products" element={<Products/>}/><Route path="products/:id" element={<ProductDetail/>}/><Route path="checkout/:id" element={<Checkout/>}/><Route path="orders" element={<Orders/>}/><Route path="orders/:id" element={<OrderDetail/>}/><Route path="support" element={<Support/>}/><Route path="profile" element={<Profile/>}/><Route path="missions" element={<Missions/>}/><Route path="missions/:id" element={<MissionDetail/>}/><Route path="admin" element={<Admin/>}/></Route><Route path="*" element={<main className="grid min-h-screen place-items-center"><div className="card text-center"><p className="eyebrow">404 / route absent</p><h1 className="mt-2 text-3xl">Target not found</h1></div></main>}/></Routes>}
